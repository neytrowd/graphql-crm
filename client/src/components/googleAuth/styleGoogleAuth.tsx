import {makeStyles} from "@mui/styles";

const styleGoogleAuth = makeStyles((theme) => ({
    box: {
        margin: '30px 0',
        display: 'flex',
        justifyContent: 'center'
    }
}));

export default styleGoogleAuth;