import {makeStyles} from "@mui/styles";

const styleStream = makeStyles((theme) => ({
    wrap: {
        padding: '15px',
        background: '#fff',
        borderRadius: '5px'
    },
    timeDots: {
        maxWidth: "112px",
    }
}))

export default styleStream;