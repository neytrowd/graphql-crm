import {makeStyles} from "@mui/styles";

const styleStatus = makeStyles((theme) => ({
    wrap: {
        minHeight: '350px',
        background: '#fff',
        borderRadius: '5px',
        padding: '25px'
    },
    delete: {
        background: '#ffefef',
        padding: '20px',
        borderRadius: '5px'
    }
}))

export default styleStatus;