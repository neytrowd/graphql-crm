import {makeStyles} from '@mui/styles';

const styleNotFound = makeStyles((theme) => ({
    box: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: '100vh',
        textAlign: 'center'
    },
    title: {
        marginBottom: '45px'
    }
}))

export default styleNotFound;