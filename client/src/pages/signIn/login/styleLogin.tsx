import {makeStyles} from "@mui/styles";

const styleLogin = makeStyles((theme) => ({
    paper: {
        textAlign: 'center',
        marginTop: '150px'
    },
    avatar: {
        margin: 'auto',
        marginBottom: '15px',
    },
    register: {
        color: '#7d7979'
    }
}));

export default styleLogin;